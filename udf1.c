#include<stdio.h>
void perirect(int l, int b)
{
    printf("perimeter of the rectangle is %d",2(l+b));
}
void main()
{
    int a,b;
    printf("enter the two values");
    scanf("%d,%d",&a,&b);
    perirect(a,b);
}