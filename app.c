#include<stdio.h>
#include<math.h>
void main()
{
int n;
printf("%d raised to the power of 2 is %d",n,pow(n,2));
printf("%d raised to the power of 3 is %d",n,pow(n,3));
printf("%d raised to the power of 4 is %d",n,pow(n,4));
}